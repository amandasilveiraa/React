import styled from "styled-components";

export const SectionContainer = styled.section`
    padding-left: 20px;
    padding-top: 20px;
    flex-grow: 1;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
`